# See wbfm_pipeline

This is in principle the same, but refactored to only run on one dataset, not a full day

Note that the original pipeline was designed to be copied into the location with the raw data.
This will instead be in the location with only the intermediate and final products, and the path to the raw data will be set in the config file.