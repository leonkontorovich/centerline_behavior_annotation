# Imutils
Contains a python file with useful image processing functions that were written for the skeleton pipeline.
At the moment:
ometiff2bigtiff, to convert ome tiff files into one big tiff file. (This functions should be merged with the one in protocols)
tiff2avi, to convert tiff files to avi.
